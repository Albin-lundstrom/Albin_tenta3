<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user[i].title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    <style>
        .admin-btn{
            margin: 0 1% 0 1%;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <a class="navbar-brand btn btn-danger text-light" href="/">Home</a>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav d-flex" style="width: 100%;">
            <% if(data[0].admin === true){ %>
                <a href="./admin" class="p-2 nav-link">Admin center</a>
             <% }; %>
              <a class="nav-link ms-auto p-2" aria-current="page" href="./logout">Logout</a>
              <% if(data[0].admin === true){ %>
                <a class="btn btn-danger admin-btn" href="./edit<%=i%>">Edit post</a>
            <% }; %>
            <% if(data[0].admin === true){ %>
                <a class="btn btn-danger admin-btn" href="./delete<%=i%>">Delete post</a>
            <% }; %>
            </div>
          </div>
        </div>
    </nav>
    <div class="d-flex flex-column">
        <div class="p-2 flex-shrink-10" style="height: 25%; max-height: 35vh; overflow: hidden;">
            <img src="/img/<%= user[i].img %>" alt="blog-img" style="width: 100%; height: 100%;" class="img-fluid img-thumbnail">
        </div>
        <div class="p-2 container text-center">
            <div class="row row-cols-3">
                <div class="row"><p>Written by: <%= user[i].authorId %></p></div>
                <div class="row"><h3><%= user[i].title %></h3></div>
                <div class="row"><p>Pusblished: <%= user[i].createdAt %></p></div>
            </div> 
        </div>
        <div class="p-2 flex-grow-10">
            <p><%= user[i].content %></p>
        </div>
    </div>
</body>
</html>